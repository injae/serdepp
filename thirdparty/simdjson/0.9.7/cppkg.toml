[simdjson]
version="0.9.7" #(require)
type="lib" #lib(default) | bin | cmake
description="Parsing gigabytes of JSON per second" #(require)
module="simdjson::simdjson" #(require) if none_module=true -> no require
url="https://github.com/simdjson/simdjson/archive/refs/tags/v0.9.7.zip" #(require)
flags="-DSIMD_DEVELOPER_MODE=OFF"
#branch="" #(optional & require git)
#link=private #default
